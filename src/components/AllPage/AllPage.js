import React from "react";
import { SAllPage, STitle } from "./styles";

const AllPage = ({ title }) => {
    return (
        <SAllPage>
            <STitle>{title}</STitle>
        </SAllPage>
    );
};

export default AllPage;
