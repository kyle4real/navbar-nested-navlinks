import styled from "styled-components";

import { v } from "../../../../../styles/variables";

export const SDropdown = styled.div`
    position: absolute;
    width: 100%;
    height: 10vh;
    background: red;
    top: ${v.headerHeight};
    cursor: initial;
`;
